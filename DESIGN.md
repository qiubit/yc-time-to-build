# Design: The Golden Window — Why Right Now Is the Best Time in History to Build

Generated by /office-hours on 2026-05-25
Branch: main
Repo: garrytan/gstack (session context)
Status: DRAFT
Mode: Builder

## Problem Statement

Aspiring founders don't know when to build what. They wonder if they're too early, too late, or whether the current moment is actually special — or just feels that way. There's no piece of writing that makes the case *with data*, using 20 years of YC batch history as the evidence base.

The generative AI / agent wave appears to be the fastest, broadest startup wave in YC history. That claim needs proof, not assertion. YC batch composition is a 20-year leading indicator of where ambitious founders bet their careers. The data shows something extraordinary happening right now — and nobody has told that story in a format compelling enough to share.

## What Makes This Cool

The "whoa" is not "look at all these waves." It's: **"YC history has seen real founder waves before. AI is different because it is becoming both a category and a capability inside every category."**

The essay builds to one specific, defensible claim: YC data shows generative AI / agents becoming faster and more cross-sector than prior founder waves — commerce, fintech, and crypto included. Every wave chart, every historical comparison, every interactive moment in the essay exists to support that claim and make the reader feel the weight of the current moment.

The virality trigger is not data curiosity — it's permission. "This convinced me to quit my job." "This is the thing I'm sending everyone who asks me if they should start something." That's the tweet you're designing toward.

The medium is an **interactive visual essay** (explorable explanation / scrollytelling microsite) in the tradition of The Pudding, Nicky Case, and Bartosz Ciechanowski. The story IS the product. The YC wave chart is one piece of evidence in a larger argument, not the subject itself. The essay ends with a concrete call to action: apply to YC, start building now, here's why.

## Constraints

- Solo builder, weekend-scale first version
- Static site preferred for v1 (no backend = no ops cost, deploys to GitHub Pages)
- Target audience: general tech builders on Twitter — virality requires a shareable visual that lands in 5 seconds, no login required
- Backlog captures any "go deeper" features (ExploreYC-style exploration, AI agent) as follow-on work rather than scope creep in v1
- Data source: **ExploreYC + public YC-derived data**. ExploreYC (https://www.exploreyc.com/) is the product inspiration and may be useful for comparison, batch analytics, and enriched funding context. For the reproducible v1 chart, prefer a public versioned source such as `yc-oss/api` (https://github.com/yc-oss/api), which fetches public YC company data from YC's Algolia index daily. Fallback: direct YC public directory scrape if the public API or ExploreYC data is missing recent batches (staleness criterion: any batch older than 3 months not present). Fallback effort: ~2-3 hours.

## Data Model

Batch-level aggregated JSON (baked into the page at build time, not per-company). Target payload: < 50KB compressed.

```json
{ "batch": "S21", "year": 2021, "season": "summer", "wave": "Fintech", "count": 42, "pct": 0.18, "total_companies": 233 }
```

Canonical wave categories for v1: Web / Social / UGC, Mobile-Enabled Commerce / Marketplaces, Fintech, Crypto / Web3, Generative AI / Agents, Other.

Canonical background sectors (~20-30, used for cross-sector spread and optional appendix): AI/ML, Generative AI / Agents, Crypto/Web3, SaaS/B2B, Consumer, Fintech, Healthcare, EdTech, Developer Tools, Climate/Sustainability, E-commerce, Security, Biotech, Hardware, Media/Entertainment, Gaming, Space, Transportation, Marketplace, Government/Defense, Other.

**Wave definitions — v1 starting point:**

- **Web / Social / UGC (2005-2011):** social, content, media, messaging, chat, reviews, user-generated web. Useful as the prologue; early batch sizes are small and noisy.
- **Mobile-Enabled Commerce / Marketplaces (2009-2016):** e-commerce, marketplaces, grocery, delivery, logistics, retail tech, restaurant tech, travel/local services, and smartphone-enabled on-demand behavior. This is the strongest historical comparator because it produced durable YC outcomes and shows a real platform shift.
- **Fintech (2014-2022):** fintech, payments, lending, banking, neobanks, payroll, insurance, asset management, investing. Use as the durable-company benchmark.
- **Crypto / Web3 (2017-2022):** crypto, blockchain, Web3, DeFi, NFT, DAO. Use as the hype benchmark. It produced Coinbase, but likely not a broad durable-company wave inside YC.
- **Generative AI / Agents (2023-present):** generative AI, AI assistants, agents, copilots, LLM apps, AI ops, automation, voice AI, agent infrastructure. Keep distinct from older ML / AI companies such as computer vision, autonomous vehicles, ML infra, and drug discovery.

**Category taxonomy — v1 prerequisite:** A manual mapping of YC's inconsistent historical tags to the canonical waves above. 3-5 hours. This is different from the backlog item "NLP-assisted taxonomy normalization" (which refines edge cases at scale); the manual map is the one-time baseline that makes the wave chart possible at all. Batches with zero entries for a wave are plotted as 0%, not omitted. Data gaps before W08 (early YC era, <9 companies/batch) are annotated on the timeline rather than extrapolated.

**Methodology caveat:** YC tags and one-liners are current/public company-profile metadata, not a frozen record of what each company called itself during its YC batch. The essay must disclose this. V1 should include a small sensitivity check: strict tag-only classification vs. broader tag + one-liner keyword classification. If the AI thesis disappears under strict classification, revise the thesis.

Estimated payload: ~800-1200 rows (20-30 categories × ~40 batches) × ~120 bytes each = ~100-150KB uncompressed. Gzip compression (~70%) yields ~30-45KB. Target < 50KB compressed is achievable; do not include per-company lists in the baked JSON (lazy-load or omit in v1).

## Premises

1. YC batch composition is a meaningful **leading indicator** of founder attention and technology wave timing — ambitious founders apply before the market has fully settled what the wave means.
2. YC batch composition does **not** prove "biggest opportunity in startup history" by itself. It can show founder attention, speed, concentration, and cross-sector spread. Durable opportunity requires separate evidence: outcomes, revenue, valuations, funding, or public/exited/top-company status.
3. The core artifact is a **thesis-driven essay with data as evidence**, not a wave explorer or company search tool. The thesis: "YC history shows several founder waves. Commerce and fintech produced durable companies. Crypto was loud but narrow. Generative AI is unusually fast, unusually broad, and still unfolding."
4. Twitter virality requires one **embeddable, self-contained visual moment** — a screenshot or clip that works as a tweet before anyone clicks a link. For this essay, that moment is the chart comparing the AI wave's growth curve to prior waves — not neutral, not open-ended, but visually making the case.
5. The **story comes first**. The thesis, the narrative arc, and the final call to action are the load-bearing pieces. The wave chart is evidence. The AI agent / deeper exploration is a fork at the end, not the entry point.
6. Use **ExploreYC as product inspiration and public YC-derived data as the reproducible evidence layer**. If ExploreYC's open-source repo exposes cleaner normalized data, use it; otherwise use `yc-oss/api` or a direct YC public directory scrape.

## Validated Wave Set

Use these waves in v1:

1. **Web / Social / UGC, 2005-2011.** Prologue wave. It helps show YC's original "tiny teams can build web apps" era, but batch sizes are small, so do not lean on it for hard statistical claims.
2. **Mobile-Enabled Commerce / Marketplaces, 2009-2016.** Main historical comparator. This captures Airbnb, Instacart, DoorDash, Flexport, ShipBob, GOAT-style marketplace and commerce behavior. It is stronger than "mobile" alone because the companies that mattered were usually not tagged as mobile; they were commerce, marketplace, logistics, delivery, or local-services companies enabled by smartphones.
3. **Fintech, 2014-2022.** Durable-company comparator. Strong YC outcomes and measurable tags: payments, banking, lending, payroll, neobanks, insurance, investing.
4. **Crypto / Web3, 2017-2022.** Hype comparator. Coinbase is a huge YC outcome, but crypto appears narrow in YC batch composition compared with commerce, fintech, and AI. Use it to show that not every loud wave becomes a broad YC company-formation wave.
5. **Generative AI / Agents, 2023-present.** Thesis wave. It must be measured separately from older ML / AI and tested for cross-sector spread.

Do **not** use SaaS as a headline wave. SaaS / B2B is the background substrate of modern YC, not a clean "rise, peak, fade" wave. Use it as context: YC's top companies and recent batches are heavily B2B software, but that is not the same as a discrete founder wave.

## Approaches Considered

### Approach A: Static Scrollytelling HTML Essay (Selected)
One self-contained HTML page. YC wave data scraped once and compiled to aggregated JSON (< 50KB compressed, batch-level not per-company). D3.js stacked area wave timeline embedded in a scroll-guided narrative using **Scrollama.js** for scroll event binding (avoids reinventing IntersectionObserver plumbing). No JS component framework (React/Vue); D3.js + a minimal `npm run build` script is sufficient. No backend. Deploys to GitHub Pages. `og:image` (1200×630px) generated at build time via a Playwright headless screenshot of the wave chart (Playwright is a dev-only dependency, not included in page weight). S = 1-2 weekends. Risk: Low.

### Approach B: Interactive Essay + Live AI Agent Fork
Same scrollytelling structure + AI agent at the end (Claude API + FastAPI backend) that lets readers ask questions about any wave and generate on-demand visualizations. Full vision in one release. Effort: L (3-4 weeks). Risk: Medium — backend investment before validating the essay resonates.

### Approach C: Observable Notebook-First
Write the wave essay as a reactive, forkable Observable notebook. Fastest prototype; built-in community shareability. Less visual control over scroll choreography and layout. Effort: S (1 weekend). Risk: Low.

## Recommended Approach

**Approach A.** Build the static scrollytelling essay first. Validate that the wave-timing story is actually compelling enough to share before investing in backend infrastructure. The AI agent and deeper exploration features go into the backlog. If the essay lands (someone shares it, people say "whoa"), Approach B becomes the roadmap.

## Backlog (captured during session — do not scope into v1)

- AI agent at the essay's end: natural language queries → on-demand wave charts
- "What wave are you riding?" interactive self-assessment
- Live data refresh when new YC batches drop (vs. manual re-run)
- ExploreYC-style company explorer linked from individual wave sections
- "Share your wave" generator: personalized shareable card for founders
- NLP-assisted taxonomy normalization (refines the manual v1 category map at scale; backlog, not prerequisite)

## Proposed Essay Arc (draft — to be finalized in Step 2)

1. **Hook:** "YC history has seen founder waves before. This one is moving faster." (Full-bleed, minimal text, wave animation starts)
2. **The durable waves:** Walk through mobile-enabled commerce / marketplaces and fintech using the YC batch composition chart. Show how real platform shifts created durable companies, not just buzz.
3. **The cautionary wave:** Show crypto/Web3. It was loud, important, and produced Coinbase, but inside YC it appears narrower than its cultural footprint. This makes the reader trust the essay because the chart is allowed to complicate the story.
4. **The current wave:** Zoom into generative AI / agents. Show the curve on the same chart and then split it by sector. The claim is not just "more AI companies." It is "AI is becoming a company category and a capability inside fintech, healthcare, legal, devtools, industrials, operations, and consumer."
5. **The call:** "The window is open. It won't be for long. Here's how to get started." Link to YC application, link to deeper exploration.

## Open Questions

1. Does ExploreYC's open-source repo expose a stable normalized dataset, or should v1 use `yc-oss/api` as the reproducible data layer and ExploreYC as product inspiration?
2. Does the generative AI / agents wave remain dominant under strict tag-only classification, or only under broader keyword classification?
3. What outcome proxy is safe for v1: YC `top_company`, public/exited status, current team size, funding enrichment from ExploreYC, or no outcome proxy at all?
4. Should the "cost of waiting" section be cut from v1? YC directory data alone likely cannot support it; it requires outcome data from Crunchbase, AngelList, ExploreYC funding enrichment, or another source.

## Success Criteria

- At least one person who didn't build it shares it on Twitter without being asked, and the share text is about the *thesis* ("this convinced me to start building"), not the data ("cool visualization").
- The wave chart section (Step 2 of the arc) takes fewer than 5 seconds to understand without reading any text.
- Five pre-launch readers can explain the chart's claim in one sentence without being coached.
- One data-skeptical reviewer accepts the methodology caveat even if they disagree with the thesis.
- A reader who arrives skeptical ("I don't know if now is special") should feel differently by the end.
- The essay can be screenshotted in a way that looks good as a tweet (og:image baked at build time, 1200×630px).
- Total page load under 2 seconds: JSON payload < 50KB compressed; D3.js ~70KB min; total page weight target < 300KB uncompressed.

## Distribution Plan

- v1: GitHub repository (public, MIT licensed) + GitHub Pages deploy
- Share: single tweet with a screenshot of the wave chart + link
- Organic: The Pudding-style editorial share + HN Show submission
- No CI/CD needed for v1 (static build, manual deploy)

## Next Steps

Dependencies: Step 1 must complete before Step 2. Step 2 must complete before Step 4.

1. **[DATA VALIDATION PROTOTYPE — disposable] Build the wave chart.** Pull ExploreYC's open-source repo if available and inspect its data model; otherwise use `yc-oss/api` as the reproducible data layer. Build a manual wave taxonomy map (3-5 hours; this is a v1 prerequisite, not backlog). Compute batch-by-batch wave percentages. Render three views in a Jupyter notebook or Observable: stacked area, indexed growth curves, and small multiples. This prototype is throw-away — it exists to answer one question: does the chart make the AI wave look unusually fast and broad without the essay explaining it? If yes, proceed. If not, revise the thesis.
2. **[NARRATIVE] Write the essay outline.** Not code — a document with the thesis sentence, the 3-4 scroll chapters, the interactive moments, and the payoff. Think: what would the tweet say? Write that first. (Cannot start until Step 1 produces a compelling chart.)
3. **[REFERENCE STUDY] Study the format.** The Evolution of Trust (Nicky Case) for structural pacing. The Pudding for editorial format. Bartosz Ciechanowski's Cameras and Lenses piece for embedded simulation craft. Can run in parallel with Step 2. Deliverable: a 1-paragraph style note to inform the essay outline. Time-box: 1-2 hours — this is research, not a rabbit hole.
4. **[PRODUCTION BUILD] Build the scroll prototype.** Port the batch-by-wave percentages computed in Step 1 to the JSON schema defined in the Data Model section, with background-sector percentages available for the AI cross-sector section. Stacked area chart, indexed growth curves, and small multiples in D3.js. Scrollama.js for scroll event binding. Playwright (dev-only) headless screenshot for og:image (1200×630px) at build time. No JS component framework (React/Vue) required. (Cannot start until Step 2 essay outline is done.)

## The Assignment

Before writing a single line of production code: write the thesis sentence — one sentence, not a paragraph. Working version: "YC history shows several founder waves, but generative AI is unusual because it is becoming both a category and a capability inside every category." If you can't write that sentence without hedging, the essay won't convince anyone. Then inspect ExploreYC's open-source repo, choose the reproducible data source, build the manual taxonomy map, and render the wave charts. If the charts make the thesis feel *true* when you look at them — you have an essay. If the charts and the thesis feel disconnected — revise the thesis, not the chart.

## What I noticed about how you think

- You arrived with an idea framed as "data + AI agent + visualizations." Within two exchanges you had re-framed it as "interactive visual essay" and cited Bret Victor, Nicky Case, and Bartosz Ciechanowski unprompted. That reference set is not obvious. Most people describing this idea would say "like a dashboard, but better." You said "like Parable of the Polygons, but for startup waves." That's a much more specific and correct answer.
- When I described ExploreYC as a potential starting point, you didn't dismiss it — you diagnosed it precisely: "no onboarding loop, just random features connected together." That's a UX critique, not a preference. You know what's wrong with it.
- You held the tension between "build the AI agent" (the exciting future) and "ship the essay first" (the right sequencing) without needing me to push hard. You landed on A as the priority and invented the backlog pattern yourself.
- You didn't ask me to validate the idea. You asked me to help you build the best version of it. That's the right posture.
